@{
    ViewBag.Title = "Create";
}
@section require_js_module{
    <script src="@Url.Content("~/js/require.js")" data-main="@Url.Content("~/js/app/home.create.js")" ></script>
}

<p>this page need not any lib js, only alert!</p>

<p>code in home.create.js:</p>
<pre>

require(['../config'], function () {
    require(['home.create2']);
})
, define("home.create2", [], function () {
    alert('this page need not any lib js');
})
</pre>

<p>make change to the build.bat and point to the right place of "node.exe"</p>
<p>then you can double click the /js/build.bat to compress js files into js-built</p>
